version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/python-test
    overwrite: true
hooks:
  BeforeInstall:
    - location: /install-dependencies
      runas: root
  AfterInstall:
    - location: /after-install
      runas: root
