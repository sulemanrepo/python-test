version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/python-test/app.py
    overwrite: true
hooks:
  BeforeInstall:
    - location: /install-dependencies
      runas: root
  AfterInstall:
    - location: /after-install
      runas: root
