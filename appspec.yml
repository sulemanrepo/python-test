version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html
hooks:
  BeforeInstall:
    - location: /install-dependencies
      runas: root
  AfterInstall:
    - location: /after-install
      runas: root
